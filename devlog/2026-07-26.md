# 개발일지 — 2026-07-26

## 작업 내용

### 1. 학습량 분배 방식 선택 추가
계획 생성 시 학습량 배분을 고를 수 있게 했다(고르게(기본)/초반집중/후반집중).
- `GoalCreatePage.tsx`: 선택 UI 추가, 편집 시 이전 선택 복원
- `AiLoadingPage.tsx`: 선택값을 생성 함수로 전달

### 2. 계획 분배 방식 및 AI 코멘트 표기 방식 정리
상황에 따라 사라지던 "AI 메이트의 한 마디"를 항상 뜨게 하고, 확정된 숫자 기준으로 코멘트를 별도 생성했다.
- `generate-plan/index.ts`: 패턴별 분배 확정 → Solar 보정 → 코멘트 별도 생성(실패 시 재시도, 그래도 실패면 기본 문구). 재배포 완료
- `PlanDetailPage.tsx`: "Powered by Solar" 표기를 실제 Solar 작성 코멘트일 때만 노출

### 3. '계획 수정하러 가기' 버튼 수정
목표일 당일 미완료 화면의 버튼을 계획 상세가 아닌 실제 편집 화면으로 연결했다.
- `IncompletePage.tsx`: `/plan/new` 편집 모드로 연결 + 필요한 계획 정보 조회 보완

### 4. DB 컬럼 추가
- `2026-07-26_distribution_pattern.sql`(신규): `milrim_plans`에 `distribution_pattern`·`ai_comment_by` 추가, 대시보드 실행 완료
- `schema.sql`: 두 컬럼 반영
