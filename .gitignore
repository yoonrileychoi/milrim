CLAUDE.md
function.md
*.pdf
*.xlsx
*.png

# 의존성 및 빌드
node_modules/
dist/

# 개발 도구 캐시
dev.log
supabase/.temp/

# 환경변수
.env
.env.local
.env.*.local

# 시연용 더미 데이터 스크립트 (실제 계정 UUID 포함 — 커밋 금지)
supabase/*_demo_dummy_data.sql

# 게스트 데모 시딩 트리거 SQL — 실행 중 잠깐 실제 계정 UUID가 들어갈 수 있어 커밋 금지
supabase/migrations/*_guest_demo_seed.sql
